export type Product = {
    _id: string,
    name: string,
    slug: string,
    category: string,
    sleeve: string,
    image: string,
    price: number,
    disc: number,
    description: string,
    excerpt: string,
    createdAt: Date,
    updatedAt: Date
}